``` bash
$> npm install
$> npm start
$> npm config set prefix ~/Software/npm
$> ls ~/.npmrc
$> npm view package
$> npm-remote-ls package
$> npm install -g package
$> npm -v list
$> npm list -g
$> npm root -g
$> npm config set proxy "http://login:mdp@add:port/"
$> npm config set https-proxy "http://login:mdp@add:port/"
$> npm run build
```

``` bash
$> node main.js
```
